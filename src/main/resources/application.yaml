# configure the server port to port 8080
server:
  port: 8080

# configure spring datasource and jpa with postgres using a database ?myDatabase? in localhost with username ?postgres? and password ?postgres?
spring:
  datasource:
    url: jdbc:postgresql://database-1.crgf0knajwzv.us-east-1.rds.amazonaws.com:5432/ideaportal
    username: postgres1234
    password: mydatabasepassword
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
